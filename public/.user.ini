; Personal/matter document video uploads (up to 600MB, including MS Teams recordings) — web document root copy for cPanel/PHP-FPM.
; Mirror root .user.ini; update both if limits change.
max_execution_time = 1800
max_input_time = 1800
memory_limit = -1
post_max_size = 650M
upload_max_filesize = 600M
